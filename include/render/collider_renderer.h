// render/collider_renderer.h
//
// Just a simple header to render colliders for debugging purposes.

#pragma once

#include "engine/collision.h"

/**
 * Renders the collider on screen.
 */
void render_collider(Collider *collider);
